# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | implementation | GSD Framework for prototyping | Use GSD for rapid MVP development | Maximizes efficiency and speed. | Yes | agent |
| D002 |  | architecture | Slice ordering for M001 | S01 (MCP lifecycle) → S02 (ReAct loop) → S03 (schema + privacy); S02 and S03 both depend on S01 but are independent of each other | S01 is the highest-risk slice because a broken STDIO transport or missed capability negotiation silently breaks all downstream work. Proving the full MCP lifecycle first gives S02 and S03 a stable, tested channel to build on. S03 is structural hardening on top of existing handlers so it carries the least risk and can run in parallel with S02 if needed. | Yes | agent |
| D003 |  | implementation | MCPOrchestrator connect() design | connect() performs the initialize handshake internally; _rpc() uses single-outstanding-request pattern | Callers get a ready client immediately after connect() with no extra ceremony. Single-outstanding-request simplifies the implementation at the cost of concurrency — acceptable for the current synchronous test harness. | Yes | agent |
| D004 | M002 | implementation | TaskScheduler inbox mechanism | asyncio.Queue (in-process) — producers push TaskItem objects directly. External queue infrastructure (Redis/HTTP) deferred. | Keeps the scheduler testable in isolation without any external service. The scheduler architecture is the thing being proven — the queue backend is an infrastructure concern for a later milestone. | Yes — when external queue backend becomes a requirement | collaborative |
| D005 | M003 | implementation | LangChain bridge strategy | langchain-mcp-adapters MultiServerMCPClient with custom headers for OAuth 2.1 Bearer token injection | langchain-mcp-adapters 1.2.7 already installed. MultiServerMCPClient supports custom headers in server config. Custom OAuth middleware layer is hand-rolled on top to handle M002 PKCE token acquisition — not replacing the library, wrapping it. | Yes — if a different auth flow is needed | collaborative |
| D006 | M005 | architecture | Vector store and embedder abstraction strategy for M005 | VectorStore and Embedder defined as Python Protocols; InMemoryVectorStore (numpy cosine similarity) and StubEmbedder (deterministic hash projection) are the default implementations | No heavy deps available (qdrant-client, sentence-transformers absent); numpy already installed; Protocol pattern keeps tests fast and production backends (Qdrant, ChromaDB) slottable without code changes | Yes — when a real embedding model or external vector DB is introduced | collaborative |
| D007 | M005 | compliance/security | Multi-tenant isolation unit for vector store namespacing | owner_id is always derived from JWT claims['sub'] by the gateway or test fixture — never accepted as a user-supplied parameter in authenticated flows | Prevents confused-deputy attacks where a user supplies another user's owner_id; consistent with M002 audience-binding pattern | No | collaborative |
| D008 | M005/S02 | pattern | How to add agent.output.final publishing to WriterAgent without breaking M003 tests | WriterAgent gains an optional bus parameter (default None); publishes to agent.output.final only when bus is set — existing tests pass an implicit None and are unaffected | Avoids forcing all existing WriterAgent tests to set up a MessageBus; optional dependency injection is the established pattern in this codebase (cf. set_sampling_client) | Yes — if WriterAgent is refactored to always require a bus | agent |
